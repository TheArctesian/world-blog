less history
yarn dev
ls
claude
z world
z
sudo nala update && sudo nala upgrade
shutdown now
btop
botp
git push
git commit -am "updated view and fixed data issues"
git add .
cd ..
bun dev
cd Webapp/
z fi
z web
byn dev
z api
clear
git commit -am "init UI and integrate finiance reports"
az account show
az account
az
z ac
pwd
cd webapp/
z wages
yazi
git commit -am "updated some UI * data"
cd UC-Investments/
cd ..l
cd UC-Organisations/
cd -
cd BDS/
claude --resume 6c9aa4ca-8945-4f30-b541-2c3ccf421254
clauclaude --resume 6c9aa4ca-8945-4f30-b541-2c3ccf421254
z webapp/
z uc
docker ps
docker p
docker compose up --build
vim .env
touch .env
cd UC-wages/
cd UC_Annual_Reports/
cd Data/
cd Campus_Foundation/
cd UC-Finances/
z uc-fin
git commit -am "updated some UI"
git commit -am "sum"
cd ucinvestments/
cd Sources/
z bds
bun install
yarn run dev
z UC-Investments/
cd
git clone https://github.com/ucinvestments/ucinvestments.git
git clone https://github.com/ucinvestments/BDS.git
git clone https://github.com/ucinvestments/UC-Finances.git
git clone https://github.com/ucinvestments/UC-wages.git
git clone https://github.com/ucinvestments/UC-Organisations
git clone https://github.com/ucinvestments/UC-Investments.git
cd ucInv/
mkdir ucInv
rm -rf ucInv/
mv UCInvestments/ ucInv/
mv ucIn/ ucInv
mv UC-Investments/ ucIn
mkdir UCInvestments
org
pro
claude upgrade
sudo apt install intune-portal
sudo nala install blender
mkdir Ardour
cd Music/
z music
z and
z admin
git pull
flatpak install GeForceNOW com.nvidia.geforcenow
flatpak remote-add --if-not-exists GeForceNOW https://international.download.nvidia.com/GFNLinux/flatpak/geforcenow.flatpakrepo
flatpak remote-add --if-not-exists GeForceNOW https://international.download.nvidia.com/GFNLinux/flatpak/geforcenow.flatpakrepo
flatpak install GeForceNOW com.nvidia.geforcenow
tty-clock -cs
tty-clock -c
tty-clock
tty
sudo nala install tty-clock
tmux
temux
copy .env
wc -l
z adm
make dev
docker kill 6686a025a5ca
make clean
docker kill b4209ff851cf
cd data/
git status
z admi
git rm --cached .claude/worktrees/agent-a76ff589
cd auracare-android/
codex
naje dev
claide
codex resume 019d6e2c-4034-7022-a8b0-6f51d322eca2
sudo npm install -g @openai/codex
cd nhs_conditions_scraper/
z auracare-api/
make down
make up
z auracare-android/
flatpak update
sudo flatpak update
git commit -am "added finances"
cd Finance/
yarn install
rm yarn.lock
rm -rf UC-Investments/
l
git clone https://github.com/TheArctesian/UC-Investments.git
cd stemforpalestine/
cd Organising/
cd School/
per
git commit -am "bleh"
cat github_activity.json
vim github_activity.json
vim git.py
touch git.py
cd git_activity/
mkdir git_activity
z step
la
z stephenokita.com/
rm Nord-Dark.ron
rm Dark.ron
rm -rf Geographical-Adventures/
cd Geographical-Adventures/
z per
rm d.bash
rm b.bash
git commit -am "init"
sudo rm -rf ./*
rm -rf ./*
docker compose exec api alembic revision --autogenerate -m
  "create_all_tables"
cd hypernous/
rm l.txt
copy l.txt
vim l.txt
cat l.txt
ls --tree > l.txt
ls --tree
ls -tree
vim Makefile
make flutter
# Install LLVM 18, Clang, and LLD
sudo apt install llvm-18 clang-18 lld-18 llvm-18-dev

# Verify installation
llvm-config-18 --version
# Update system
sudo apt update && sudo apt upgrade

# Add LLVM GPG key
wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key | sudo apt-key add -

# Add repository (replace 'jammy' with your Ubuntu codename)
sudo add-apt-repository "deb http://apt.llvm.org/jammy/ llvm-toolchain-jammy-18 main"
sudo apt update
llvm
sudo apt-get install -y libstdc++-14-dev
z hyp
cp .env.example .env
la -la
ls -l
ls -la
cd backend/
./aw-qt
cd activitywatch/
open activitywatch-v0.13.2-linux-x86_64.zip
z down
sudo apt install activtiywatch
cat docker-compose.yml
cat README.md
cd frontend/
git commit -am "fix node version"
cat .bashrc
flutter
cd development/
vim config.fish
z fish
bash
exit
bash d.bash
vim d.bash
sudo bash b.bash
vim b.bash
cat b.bash
sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev libstdc++-12-dev
sudo apt install android-studio -y
sudo apt update
sudo add-apt-repository ppa:maarten-fonville/android-studio
sudo apt install openjdk
sudo apt install openjdk-18-jdk
git clone https://github.com/TheArctesian/hypernous.git
reboot
man flatpak
git commit -am "added ip analytics"
git commit -am "added load bar"
git commit -am "updated analytics with IP mode"
p
gh auth login
git commit -am "added analytics"
git commit -am "reset lauout still doesn't work but added new rendeing options for the grpahs, added country filter and loading screens for the pharamcy pages"
git commit -am "changed defualt loading for pathways and questionaires fixed map loading issue, also testing deployment"
cat .env
git log
git commit -am "redeploy test"
vim README.md
git commit -am "added analytics & blobs"
cd a
git commit -am "changed node version"
git commit -am "changed logo"
ckear
echo company description, availble positions, demo equipment
git commit -am "fix map + ownership map"
cd workflows/
cd .github/
cd src/
cd static/
cd postcodes/
rm -rf ukpostcodes\(1\)/
cat uk_active_postcodes.csv
cd output/
cd admin/
cd post_code_scraper/
cd ,,
cat ,env
cd auracare-api/
z priv
cd privConf/
lls
[er
git clone https://github.com/TheArctesian/privConf.git
git commit -am "3d!"
git clone https://github.com/SebLague/Geographical-Adventures
cd world-blog/
git clone https://github.com/thearctesian/world-blog
rm -rf world-blog/
git reset
git stash
npm run textures:build -- --refetch
open globe-texture-ultra.jpg
npm run textures:build
cat ukpostcodes.csv
git commit -am "rework"
cd auracare-app-agg/
nmap -sn 192.168.4.0/24
ip neigh
arp
nmap -sn 192.168.1.0/24
sudo apt install nmap
nmap
cd NICE_Scraper/
z nhs
cleasr
zcc
cat config.fish
mv uk_active_postcodes.csv ../auracare-app-agg/admin/
sudo rm uk_active_postcodes.csv
rm uk_active_postcodes.csv
mkdir post_code_scraper
git config pull.rebase false
pull
z docker
claude auth status
git push --set-upstream origin staging
git commit -am "updated referall record system"
z an
z agg
git commit -am "changed info text to be centered"
git commit -am "changed patient fill out page"
mv NICE_Scraper/ ../
mv nhs_conditions_scraper/ ../
git checkout -b staging
git commit -am "update email template"
c
;s
z aurcare
neofetch
gpufetch
cpufetch
sudo apt install cpufetch
sudo bash build-apk.sh
cd scripts/
git commit -am "abillity to edit referral content"
rm NEW_SCHEMA.md
sudo rm hs_err_pid49.log
sudo rm hs_err_pid1.log
rm hs_err_pid1.log
.
cd .
git checkout main
git push --set-upstream origin dev
git commit -am "more comprehensive ability to edit referrals"
sudo nala install steam
cd auracare-a
LS
poe
git checkout -b dev
git commit -am "fixed last min issues"
git commit -am "some flurish"
docker compose down --remove-orphans
git commit -am "fixed 500 issue for enum on submit of IP"
git add
git commit -am "adding new things"
cd ip/
cd domains/
cd app/
sudo build-apk.sh
vi .env
z ad
● Done. The Wasm build now points to
  http://host.docker.internal:5000 (your local API) via the
  API_BASE_URL env var. Run make dev to rebuild — the generated
  PlatformBaseUrl.wasmJs.kt will pick up the new URL, and the
  medication assist will hit your local API which has the BNF
  enrichment + all 1,771 drugs loaded.

  To revert back to the Azure dev API later, just set API_BASE_URL
  in your shell or .env file, or remove the env var line from
  docker-compose.yml.
z docke
vim ca-auracare-api-dev-AutoDeployTrigger-a30be8db-50e5-468e-bacb-3a23ea17ec5c.yml
vim ci.yml
clea
cd an
cd ap
git add /
git add  .
z as
cd admin/l
mv nhs_conditions_scraper/ auracare-app-agg/
mv NICE_Scraper/ auracare-app-agg/
py scrape_cks.py
cd scrapers/
cd scraper/
z NICE
python3 scrape_treatment_summaries.py
python3 scrape_interactions.py
python3 scrape_bnf.py
git add ,
codex resume 019cf6be-919e-79b1-8d3b-7cd6c2840291
grep -irs --binary-files=without-match "BASE_URL"
grep -irs "BASE_URL"
grep -isnR "BASE_URL"
grep -inR "BASE_URL"
grep -iR "BASE_URL"
grep -aER "BASE_URL"
grep -aER "BASE"
z build
cls
git commit -am "api"
bun build
git commit -am "t
"
git puhsh
git commit -am "ajsdkjaslkjads"
git config pull.rebase false  # merge
git commit -am "working!"
git ad d.
Exclude recurrent AOM, pregnancy in under-16s, immunosuppression, high-risk co-morbidity, temperature over 39C, and urgent escalation.
cd Drugs/
mkdir Drugs
vim drugs.json
cat drugs.json
mkdir NICE_Scraper
cmatrix -C magenta
clearr
c;ear
cd auracare-api
zco
zel
BTOP
git commit -am "some resdues from migration"
codex resume 019cdd5c-55cd-7111-840d-3850849eb313
git commit -am "bye bye bloat may regret that later but can always go back to it. SEED SCRIPTS ARE HERE"
docker compose down
z snow
docker
git commit -am "hey this is looking pretty good"
z ..
mv pro.kdl claude.kdl
mv cel.kdl codex.kdl
cel
vim cel.kdl
cp pro.kdl cel.kdl
which nvim
vim config.kdl
z zel
z  zel
z abd
xclip -selection clipboard -t TARGETS -o
wl-paste --list-types
sudo apt install wl-clipboard
cd cosmic-epoch/
git clone https://github.com/pop-os/cosmic-epoch.git
z open
git commit -am "pre migration save"
npx sv create
bun
git clone https://github.com/auracarehq/admin.git
zed
git commit -am "some shit don't really rmbr"
git commit -am "some UI fixes and langauge shit"
grep -aER "Use this when"
grep -aER
grep -AeR
git commit -am "save post critque"
git commit -am "save, after critque"
rm l.md
vim l.md
git commit -am "added make file"
git commit -am "fuck my life"
sudo rm -rf .gomodcache/
rm -rf .gomodcache/
rm -rf .gocache/
z mjs
cd docs/
cd migrations/
cd test
cd style/
z tests
ls -t
cd tests/
z scaper
git commit -am "overhaul"
git commit -am "questionaires"
git push --set-upstream origin questionaires
git checkout -b questionaires
git commit -am "baaaaaa"
git commit -am "some remining fixes"
vim docker-compose.yml
cd snowstorm/
rm -rf transcription/
rm -rf TestHardware/
rm TestHardware/
git clone https://github.com/IHTSDO/snowstorm.git
make de
git commit -am "big UI fixes"
docker system prune -a
git commit -am "make gpu selection configurable and improved docs"
z cor
make run
ac ac
lz
git commit -am "added docker"
cd CorridorKey/
make
mkae
:q
cat docker-run.sh
git clone https://github.com/TheArctesian/CorridorKey.git
docker purge
docker purge -a
git commit -am "uti fixes"
docker compose run --rm -e 'PYTHONPATH=/app' api python scripts/pathway/refresh_uti_pathway.py
docker kill f01820751262
git commit -am "pathways fixes"
docker compose run --rm build ./gradlew :composeApp:compileKotlinWasmJ
jq
cd auracare-api
docker compose run --rm api python scripts/pathway/update_uti_layout_hint.py


docker compose run --rm api python scripts/pathway/update_uti_layout_hint.py

sudo chown -R okita:okita /home/okita/Scripts/Work/AC/auracare-app-agg/auracare-android/build /home/okita/Scripts/Work/AC/auracare-app-agg/auracare-android/composeApp/build
# Install using npm
sudo npm install -g @openai/codex
# Install using npm
npm install -g @openai/codex
mv auracare-api/ auracare-app-agg/
mv auracare-android/ auracare-app-agg/
mv auracare-app/ auracare-app-agg
mv app/ auracare-app
mkdir app
rm -rf main/
mkdir main
git commit -am "pushes to make this work"
docker kill b81df5d90e98
cd composeApp/src/commonMain/kotlin/com/example/auracare_android/ui/consultation/
z acls
git commit -am "cardiff"
yarn run build
vim places.json
cd lib/
cd routes/
z map
cd kotlin/
cd jsMain/
cd composeApp/
npm install
npm run dev
rm package-lock.json
npm run build
docker kill ffbe42809909
docker kill 09afb8304337
make fun
git commit -am "ip"
got push
[1;5A
docker compose build
Add a third card to the existing two-card layout:
## 1. Consultation Type Selector
---
Add a third consultation mode called **"PGD"** alongside the existing "Questionnaire" and "Pathway" options on the consultation type selection screen. PGD mode lets a pharmacist select an NHS Patient Group Direction, work through its clinical checklist (inclusion/exclusion criteria, cautions, actions, advice), mark each item as met/not met/noted, and apply the results to a referral.
## Overview
make dev# Frontend Prompt: PGD Mode — Consultation Type
cd pharmacy-first/
z notes
git commit -am "more london photos"
z wall
fish
exa --icons
exa
chafa fm13_aqua_leela_and_stephen.png
cd framework13/
cd Desktop/
chafa
sudo apt install chafa
models
llmfit
curl -fsSL https://llmfit.axjns.dev/install.sh | sh
git commit -am "added claude.md"
cat acne.md
cd conditions/
cd medicines/
curl -X 'GET' \
  'http://127.0.0.1:8000/tasks/2e4eb7e01937' \
  -H 'accept: application/json'
cat .gitignore
curl -X POST http://localhost:8000/conditions/scrape
git clone https://github.com/auracarehq/nhs_conditions_scraper.git
open .
cd notes/
git commit -am "updated pathways"
vim .
git commit -am "reduced size of text for docs on android"
make build
z Android/
sudo apt install codex
pp
[p
git commit -am "some layout fixes"
bash build-apk.sh
cat build-apk.sh
sudo apt install v4l-utils
v4l2-ctl
git commit -am "little issue on consultations"
git commit -am "fix styles"
git commit -am "polish to documentations"
sudo apt remove nano
sudo apt uninstall nano
git commit -am "polish to pathways"
docker run
cd build/
vim
cd fish/
z conf
cd ...
wrk
z brave
conf
nano
z src/
z ste
kill -CONT 135561
run kill -CONT 135561
suspend
git commmit -am "some shit"
git commit -am "post consultations half way"
git commit -am "consultations save"
./build-apk.sh
sh build-apk.sh
git commit -am "updated some things"
git commit -am "hosting"
git commmit -am "azure"
sudo apt install gh
vim hosts
vim hostname
vim locale.conf
cd /etc
git commit -am "added bettwe graph"
git commit -am "updated aom + graph layout"
docker compose run --rm api python -c "
from app.core.db.session import SessionLocal
from app.domains.pathways.models.pathway import Pathway
db = SessionLocal()
p = db.query(Pathway).filter(Pathway.code == 'PF_AOM_001').first()
if p:
    print(f'Deleting pathway {p.id}')
    db.delete(p)
    db.commit()
else:
    print('Not found')
db.close()
"

grep -r ".n"
# Delete the existing pathway and re-seed
docker compose run --rm api python -c "
from app.core.db.session import SessionLocal
from app.domains.pathways.models.pathway import Pathway
db = SessionLocal()
p = db.query(Pathway).filter(Pathway.code == 'aom').first()
if p:
    print(f'Deleting pathway {p.id}')
    db.delete(p)
    db.commit()
db.close()
"
docker compose run --rm api python -m scripts.pathways.seed_aom

git commit -am "UI fixes"
git commit -am "aom has cam let me integrate the rest now"
git push --set-upstream origin merge_pathways_transcription

git commit -am "does not work"
docker compose up --build -d
docker compose run --rm -p 8501:8501 api streamlit run mock/app.py --server.address 0.0.0.0

git checkout -b merge_pathways_transcription
cd mock/
cd voicebox/
git clone https://github.com/TheArctesian/voicebox.git
rm -rf voicebox/
vim CHANGELOG.md
vim CONTRIBUTING.md
cat CONTRIBUTING.md
z voice
make setup
sudo npm i -g bun
sudo nala install bun
# Clone the repo
git clone https://github.com/jamiepine/voicebox.git
cd voicebox

# Setup everything
make setup

# Start development
make dev
git commit -am "aom is done? in mock mode so but with streamlit on 8051"
git push --set-upstream origin feature/pathways_logic

git checkout -b feature/pathways_logic
# Seed all 7 pathways
docker compose run --rm api python scripts/seed_all_pathways.py


git checkout feature/workflow
git fetch
git branch -l
git checkout
open PRN00936_ii_pharmacy-first-clinical-pathways-version-2.5\(1\).pdf
sudo apt install xclip
copy
z pharmacy-first-models/
make logcat
git commit -am "referalls"
git commit -am "mock screen for pathways"
claude mcp add pdf-reader -- npx @sylphx/pdf-reader-mcp
git commit -am "billing, session stuff"
git commit -am "billing fixes"
git commit -am
z wal
vim dbv3.sql
copy dbv3.sql
cd db/
cd core/
cd. .
cd app/admin/
z chart
git commit -am "added fetching billing, patients and sessions by pharmacy & pharmacist"
docker compose run --rm --service-ports build ./gradlew :composeApp:wasmJsBrowserDevelopmentRun
git commit -am "save 5"
z andr
git commit -am "save 4"
git commit -am "save"
lear
z note
l,s
az account get-access-token --resource https://ossrdbms-aad.database.windows.net --query accessToken --output tsv
git commit -am "pharmcy + pharmasit update"
docker compose run --rm --service-ports build ./gradlew :composeApp:wasmJsBrowserDevelopmentRun

curl -X 'POST' \
  'http://localhost:5000/pharmacists/gphc/search' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "first_name": "Alex",
  "last_name": "",
  "registration_number": ""
}'
curl -X 'POST' \
  'http://localhost:5000/pharmacists/gphc/search' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "first_name": "Alex"
}'
curl -X 'POST' \
  'http://localhost:5000/pharmacists/gphc/search' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "first_name": "Alex",
  "last_name": "string",
  "registration_number": "string"
}'
cd app/domains/pharmacists/
sudo apt install postgresql-client-common
psql
az postgres flexible-server execute \
  --name auracare-api-dev \
  --admin-user auracare_pg_admin \
  --admin-password '^qaB4mCHvtcKzHDjEe5d' \
  --database-name postgres \
  --querytext "
ALTER TABLE pharmacy_staff DROP CONSTRAINT IF EXISTS staff_pharmacy_fk;
ALTER TABLE \"Session\" DROP CONSTRAINT IF EXISTS session_pharmacy_fk;
ALTER TABLE users DROP CONSTRAINT IF EXISTS user_pharmacy_fk;

DROP TABLE IF EXISTS pharmacies CASCADE;

CREATE TABLE pharmacies (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    registration_number VARCHAR(20) NOT NULL UNIQUE,
    trading_name VARCHAR(300) NOT NULL,
    address_line_1 VARCHAR(255) NOT NULL,
    address_line_2 VARCHAR(255) NULL,
    address_line_3 VARCHAR(255) NULL,
    town VARCHAR(100) NULL,
    county VARCHAR(100) NULL,
    country VARCHAR(100) NULL,
    postcode VARCHAR(10) NOT NULL,
    region VARCHAR(100) NULL,
    owner_name VARCHAR(300) NULL,
    premises_type VARCHAR(100) NULL,
    internet_pharmacy_logo VARCHAR(10) NULL,
    status_at_snapshot VARCHAR(50) NULL,
    current_status VARCHAR(50) NULL,
    initial_registration_date DATE NULL,
    renewal_date DATE NULL,
    expiry_date DATE NULL,
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMP(0) WITHOUT TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP(0) WITHOUT TIME ZONE DEFAULT NOW()
);

CREATE INDEX idx_pharmacies_reg_number ON pharmacies(registration_number);
CREATE INDEX idx_pharmacies_postcode ON pharmacies(postcode);
CREATE INDEX idx_pharmacies_trading_name ON pharmacies(trading_name);

ALTER TABLE pharmacy_staff ADD CONSTRAINT staff_pharmacy_fk FOREIGN KEY (pharmacy_id) REFERENCES pharmacies(id);
ALTER TABLE \"Session\" ADD CONSTRAINT session_pharmacy_fk FOREIGN KEY (pharmacy_id) REFERENCES pharmacies(id);
ALTER TABLE users ADD CONSTRAINT user_pharmacy_fk FOREIGN KEY (pharmacy_id) REFERENCES pharmacies(id);
"

cat Registered-Premises-List-31-12-2025.csv
cd chartdb/
z Open
cd Open
cd Downloads/
sudo rm Registered-Premises-List-31-12-2025.csv
rm Registered-Premises-List-31-12-2025.csv
open Registered-Premises-List-31-12-2025.xlsx
cd Pharmacies/
mkdir Pharmacies
cat patel_all.csv
cat test.csv
cd GPhC/
mkdir GPhC
cd transcription/
mkdir transcription
git commit -am "update auth"
az postgres flexible-server execute \
  --name auracare-api-dev \
  --admin-user auracare_pg_admin \
  --admin-password '^qaB4mCHvtcKzHDjEe5d' \
  --database-name postgres \
  --querytext "ALTER TABLE patients ALTER COLUMN nhs_number DROP NOT NULL;"

az sql db
az sql db query \
--server auracare-api-dev
sudo apt install openjdk-25-jre-headless
java
sudo apt install jdk
curl -LsSf https://astral.sh/uv/install.sh | sh
sudo apt install uv
# 1. Context7 — live documentation lookup
claude mcp add context7 -- npx -y @upstash/context7-mcp@latest

# 2. Sequential Thinking — structured reasoning
claude mcp add sequential-thinking -- npx -y @modelcontextprotocol/server-sequential-thinking

# 3. Android MCP Server — ADB device control (requires cloning + uv)
# This one needs a bit more setup since it's a Python project

git commit -am "more photos"
git commit -am "nav rail"
git diff
cargo install
rustup default stable
git clone --depth=1 https://github.com/ProtonMail/WebClients.git WebClients
vim ./scripts/setup.sh
sudo sh ./scripts/setup.sh
sudo apt install rustup
rustc
node
sh ./scripts/setup.sh
./scripts/setup.sh
cd protondrive-linux/
git clone https://github.com/DonnieDice/protondrive-linux.git
cd OpenSource/
z Sc
z os
sudo flatpak install proton-drive.flatpak
flatpak install proton-drive.flatpak
docker compose run --rm build ./gradlew :composeApp:wasmJsBrowserDev
find . -name "*.jpeg" -type f -exec wc -l {} +
find . -name "*.jpeg"
cd raw/
raw
git commit -am "more from further back"
cd wallpapers/
git commit -am "mock ui view"
git clone https://github.com/chartdb/chartdb.git
cd Scripts/
git commit -am "added younger photos"
z aura
sudo apt install btop
gotop
imgcat fm13_21st_bday.png
nix-env -iA nixpkgs.imgcat
imgcat
cmatrix -C green
cmatrix -C white
cmatrix -C sd
cmatrix -C
cmatrix -C yellow
cmatrix -C cyan
cmatrix -C orange
cmatrix -C Red
cmatrix -C blue
cmatrix -h
cmatrix -a
cmatrix -m
cmatrix -c
cmatrix -r
cmatrix -b
cmatrix
sudo apt install cmatrix
git commit -am "finished the rest?"
mv PF\ Referral\ Process\ \&\ Communication\ Flow\ TEMPLATE.pdf ~/Scripts/Work/AC/auracare-api/notes/
mv pharmacy-first-blank-form-v1.0.pdf ~/Scripts/Work/AC/auracare-api/notes/
git commit -am "addded the pathways"
docker compose run --rm api python scripts/seed_sore_throat_pathway.py

docker compose run --rm api python scripts/seed_insect_bites_pathway.py

docker compose run --rm api python scripts/seed_all.py
docker compose run --rm api python scripts/seed_all.py

[200~docker compose run --rm build ./gradlew :composeApp:wasmJsBrowserDev
docker compose run --rm build ./gradlew :kotlinWasmUpgradeYarnLock && docker compose run --rm build ./gradlew:composeApp:wasmJsBrowserDevelopmentRun
docker compose run --rm build ./gradlew :composeApp:wasmJsBrowserDevelopmentRun
docker compose run --rm build ./gradlew :composeApp:run
claude mcp add --transport http figma https://mcp.figma.com/mcp
nvim
nvim .
cd nvim/
cd ghostty/
nix-env -iA nixpkgs.neovim
sudo apt remove neovim
sudo apt remove nvim
nvim -h
nvim -v
rm -rf ~/.config/nvim/.git
git clone https://github.com/LazyVim/starter ~/.config/nvim
mkdir nvim
# required
mv ~/.config/nvim{,.bak}

# optional but recommended
mv ~/.local/share/nvim{,.bak}
mv ~/.local/state/nvim{,.bak}
mv ~/.cache/nvim{,.bak}
sudo apt update -y && sudo apt upgrade -y
cd auracare-and
git commit -am "init and that"
bash setup-azure.sh
sh setup-azure.sh
az login
az account login
curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
sudo apt install azure-cli
sudo apt install az
docker compose run --rm api python -m scripts.seed_uti_pathway
rm -rf nordify/
rm nordify/
cd Pictures/
sudo nala upgrade
git clone https://github.com/thearctesian/nordify
ghostty
vim config
sudo apt install go
go install
go
sudo apt install golang
down
git commit -am "update style.md"
git
mkdir framework13
mkdir raw
z Pictures/
git push origin main
git commit -am "user auth"
sudo systemctl enable --now waydroid-container
waydroid
sudo apt install curl ca-certificates -y
curl https://repo.waydro.id | sudo bash
sudo apt install waydroid -y
sudo waydroid container start
waydroid show-full-ui
find . -name "*.ts" -type f ! -name "*.test.ts" -exec wc -l {} + | awk '$1 > 500 && !/total$/ {print; count++} END {print "\n" count, "files over 500 lines"}'
find . -name "*.ts" -type f ! -name "*.test.ts" -exec wc -l {} + | awk 'END {print "Average:", total/(NR-1), "lines"} {total+=$1}'
find . -name "*.ts" -type f ! -name "*.test.ts" -exec wc -l {} + | sort -n | tail -2 | head -1
find . -name "*.ts" -type f -exec wc -l {} + | sort -n | tail -2 | head -1
cd openclaw/
z opencl
git clone https://github.com/openclaw/openclaw.git
mkdir OpenSource
z scr
oss
cd startpage/
git clone https://github.com/TheArctesian/startpage
oer
z sp
z sec
git commit -am "added the azure connections"
docker compose down && docker compose up --build
sudo rm -rf db/redis db/postgres db/azurite

flatpak install flathub io.github.brunofin.Cohesion
git commit -am "fixed phramacy monitors"
git commit -am "updated model and fixed imports"
git commit -am "updated test suite"
wc
git clone https://github.com/TheArctesian/wallpapers.git
rm -rf wallpaper/
rm wallpaper/
cd wallpaper/
git commit -am "changed the design to be domain based"
git push --set-upstream origin domain_based_change
git checkout -b domain_based_change
open app.py
z stephen
open drawSQL-pgsql-export-2026-02-02.sql
vim drawSQL-pgsql-export-2026-01-30.sql
copy drawSQL-pgsql-export-2026-02-02.sql
cat drawSQL-pgsql-export-2026-02-02.sql
sudo dd if=pop-os_24.04_amd64_nvidia_22.iso of=/dev/sda bs=4M status=progress conv=fsync
git commit -am "added test suite + api sketch"
claud
cleara
git push --set-upstream origin API-PLAN-SKETCH
git checkout -b API-PLAN-SKETCH
claude mcp add --scope project scaffold \
  npx @modelcontextprotocol/server-template
claude mcp add --scope project eslint \
  npx @modelcontextprotocol/server-eslint
claude mcp add --scope project python \
  npx @modelcontextprotocol/server-python
claude mcp add --scope project filesystem \
  npx @modelcontextprotocol/server-filesystem \
  --env ROOT_PATH=.
claude --dangerously-skip-permissions
ls --treee
curl http://localhost:5000/redoc
docker ls
cat Readme.md
vim pro.kdl
z zellij
zellij
nix-env -iA nixpkgs.zellij
nix-env -iA nixpkgs.yazi
nix-env -iA nixos.yazi
nix-env -iA yazi
rm 2026-01-27\ 16-33-55.mp4
nix
vim nix.conf
cd /etc/nix
sh <(curl -L https://nixos.org/nix/install) --daemon
zoxide
sudo apt install ffmpeg 7zip jq poppler-utils fd-find ripgrep fzf zoxide imagemagick
apt install ffmpeg 7zip jq poppler-utils fd-find ripgrep fzf zoxide imagemagick
git commit -am "rm __pychace__"
sudo rm -rf __pycache__/
rm -rf __pycache__/
git commit -am "added swagger + claude.md + build flags for non docker"
git rm -rf __pycache__/
cat app.py
cd api/
cat CLAUDE.md
sudo apt install tree
tree
docker run hello-world
sudo apt remove $(dpkg --get-selections docker.io docker-compose docker-compose-v2 docker-doc podman-docker containerd runc | cut -f1)
pip install -r requirements.txt
mkdir tests
mkdir api
cd pathways/
vim app.py
git commit -am "init api"
rm pathways.zip
pclear
ip a
cat OAS.json
open OAS.json
vim OAS.json
cd Spine/
cd NHS/
zip -r pathways.zip pathways/
man zip
zip pathways/ pathways.zip
zip pathways/
cd allergy/
vim Readme.md
mv pharmacy-first/ notes/
mv pathways/ notes/
mv NHS/ notes/
mkdir notes
git clone https://github.com/auracare-org/auracare-api.git
git clone https://github.com/auracare-org/pharmacy-first-models.git
git commit -am "added splash + sign in/up UI Screen"
adb -l
adb devices
adb -d
sudo apt install java
adb
sudo apt install adb
unzip Auracare\ Design\ System_icon.zip
rm Auracare\ Design\ System_icon.zip.gz
unzip Auracare\ Design\ System_icon.zip.gz
gzip Auracare\ Design\ System_icon.zip
cat Export.csv
open Export.csv
flatpak install flathub io.github.Figma_Linux.figma_linux
cd androidMain/
./gradlew
git clone https://github.com/auracare-org/auracare-android.git
rm -rf auracare-android/
https://github.com/auracare-org/auracare-android.git
cd AC/
z pic
cd src/lib/
cd nice_pathways/
ac
cd Documents/Second\ Brain/
git commit -am "update css"
rm .playwright-mcp/*
z stephe
git commit -am "update responses"
lg
rm REINSTATE\ PEYRIN!\ \(Responses\)\ -\ Form\ responses\ 1.csv
py process_responses.py
sh move_csv_files.sh
py move_csv_files.sh
cd acute_sinusitis/
rm acute_sinusitis.zip
rm -rf PharmFirstJan2026Docs/
mkdir UTI
cd PharmFirstJan2026Docs/
mkdir PharmFirstJan2026Docs
sudo dd if=pop-os_24.04_amd64_generic_22.iso of=/dev/sda bs=4M status=progress conv=fsync
lsblk
vim .claude.json
claude mcp help
claude mcp playwright
claude mcp remove chrome-devtools
claude mcp remove chome-devtools
claude mcp list
claude mcp
claude mcp add playwright npx @playwright/mcp@latest
claude mcp add chrome-devtools --scope user npx chrome-devtools-mcp@latest
sudo npm i -g yarn
node -v
sudo n stable
n stable
sudo npm i -g n
npm i -g n
npm i n
npm i -g yarn
npm
yarn
cd Videos/
z videos
git clone https://github.com/free-tv/iptv
curl -fsSL https://rams.ai/install | bash
du
git commit -am "rm video"
cd Tech-JusticeLab/
git clone https://github.com/techjusticelab/Tech-JusticeLab.git
cd tjl/
mkdir tjl
sudo nala install python3-pip
sudo apt install python3-pip
pip
python3-pip
sudo nala update -y && sudo nala upgrade -y
sudo apt install nala
sudo apt update --y && sudo apt upgrade --y
bash conv.sh ./
vim conv.sh ./
cd Leela/
convert
sh conv.sh ./
vim conv.sh
sudo apt install libheif-examples
ffmpeg
sh conv.sh
touch conv.sh
git puish
git commit -am "added more"
echo hello world
git clone https://github.com/TheArctesian/world-blog.git
zed .
cd stephenokita.com/
rm Organization\ Endorsement_\ Sign\ the\ Open\ Letter\ to\ Reinstate\ Peyrin!\ \(Responses\)\ -\ Form\ responses\ 1.csv
py process_org_responses.py
batcat move_csv_files.sh
batcat
sudo apt install bat
cat move_csv_files.sh
vim notable_signatories.json
sl
cd Organising/stemforpalestine/
cd Project/
z stem
sudo apt install zellij xh fd dunst
z fis
cd fis
cd .config/
eza
sudo apt install eza
sudo apt install exa
z wrk
vim fish/config.fish
chsh -s /usr/bin/fish
sudo apt install pfetch
rm types.ts
cat types.ts
rm registry.ts
vim registry.ts
cd acute_s
git checkout rewrite
git branch
git clone https://github.com/auracare-org/pathways.git
mkdir AC
cd Work/
mkdir Work
zoxide init fish | source
git commit -am "fixed spelling"
ping -c 3 google.com
sudo apt install npm
np,
git clone https://github.com/TheArctesian/stephenokita.com
mv Dark.ron Scripts/Personal/
zedit
omf install boxfish
sh omf.sh
cd dotfiles/
./gitConf
cd Git/
git clone https://github.com/TheArctesian/dotfiles.git
z Downloads
zoxide  init fish
git commit -am "stem4pal"
rm 'REINSTATE PEYRIN! (Responses) - Form responses 1.csv'
rm 'Organization Endorsement_ Sign the Open Letter to Reinstate Peyrin! (Responses) - Form responses 1.csv'
python3 process_responses.py
python3 process_org_responses.py
git clone https://github.com/TheArctesian/stemforpalestine.git
